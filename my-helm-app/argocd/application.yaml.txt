apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: myapp-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/<org>/<repo>.git'
    targetRevision: main
    path: my-helm-app/charts/myapp
    helm:
      valueFiles:
        - values.yaml
        - values-secret.yaml
      parameters:
        - name: secret.env
          value: dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
