replicaCount: 1

image:
  repository: nginx
  tag: latest

config:
  app_name: my-helm-app

# Secret references (base64 decoded at runtime)
secretRefs:
  dbUser: {{ .Files.Get "values-secret.yaml.enc" | fromYaml | get "secrets.db_user_b64" | b64dec }}
  dbPass: {{ .Files.Get "values-secret.yaml.enc" | fromYaml | get "secrets.db_pass_b64" | b64dec }}
